const net = [[0, 6, 20, 20],
    [6, 0, 20, 20],
    [20, 20, 0, 12],
    [20, 20, 12, 0]]

const node = 4
const swch = node -2

function netCable(net:number[][]): number {
    let cableLen=0
    return cableLen
}

console.log(netCable(net));
